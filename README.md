# TASK DAY 5
This task is all about creating a JSON file and creating an object in javascript and Iterating through them using all types of for loops
* for
* for in
* for of
* forEach

This repository has 2 files : _**resume.JSON**_ and  _**iterateResume.js**_

### 1. resume.JSON

This file is a JSON file which contains my resume in JSON format

### 2. itertaeResume.js

This file is a Js file which has a object named resume which also has all properties exactly similar to the JSON file but used for iterating using all four types of for loops

